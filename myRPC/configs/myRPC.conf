# myRPC server configuration file
# Located at: /etc/myRPC/myRPC.conf
#
# Reload at runtime: kill -HUP <pid>  or  systemctl reload myrpc-server

# TCP/UDP port to listen on
port = 1234

# Socket type: stream (TCP) or dgram (UDP)
#socket_type = dgram
socket_type = stream
